import { Model } from "@mean-expert/model";

/**
 * @class loopbackapi
 */
@Model({
    hooks: {
    },
    remotes: {
    },
})
class loopbackapi {
    constructor(public model: any) {
    }

}
module.exports = loopbackapi;
